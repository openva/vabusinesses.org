# NOTE: nothing reads this file at present.
#
# Analysis is done by SonarCloud's own Automatic Analysis, which scans the
# repository server-side and ignores this file entirely -- it has its own
# optional .sonarcloud.properties instead. The CI scan that did read this was
# removed from .github/workflows/ci.yml because the two cannot both run:
# "You are running CI analysis while Automatic Analysis is enabled."
#
# It is kept because it is correct, and because restoring CI-based analysis is
# then a matter of turning Automatic Analysis off in the SonarCloud UI (project
# Overview -> Choose your Analysis Method) and re-adding the job. Doing so also
# restores the two things Automatic Analysis cannot do: read the coverage report
# named below, and honour the exclusions that keep vendor/ out of the metrics.

sonar.organization=openva

# must be unique in a given SonarQube instance
sonar.projectKey=openva_vabusinesses.org
# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=vabusinesses
sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set. 
sonar.sources=.

# Third-party and generated files are not ours to fix, and scanning them dilutes
# the project's own metrics -- vendor/ alone is over a thousand PHP files.
sonar.exclusions=vendor/**,node_modules/**,data/**,templates_c/**,mini-default.min.css,municipalities.geojson,composer.lock,package-lock.json

# Tests are exercised, not measured for coverage.
sonar.test.exclusions=deploy/tests/**

#sonar.php.tests.reportPath=test-report.xml
sonar.php.coverage.reportPaths=coverage-report.clover
